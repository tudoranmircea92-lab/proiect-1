# Frontend (React)

## Setup
```bash
cd frontend
npm install
```

## Run
```bash
npm start
```

The UI integrates with backend `http://127.0.0.1:8000` and supports:
- file upload + scan preview
- training trigger
- feature importance charts
- live logs
